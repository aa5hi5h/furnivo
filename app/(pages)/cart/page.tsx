import CartPage from '@/components/cart-page';

export const metadata = {
  title: 'Shopping Cart | FURNIVO',
  description: 'Review your cart and proceed to checkout',
};

export default function Page() {
  return <CartPage />;
}